# Supabase Configuration
# Get these values from https://app.supabase.com/project/_/settings/api

# Supabase URL
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Supabase Anon/Public Key (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase Service Role Key (NEVER expose to browser, server-side only)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# JWT Secret for Mastery Token signing (generate a random string)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Sentry, Analytics, etc.
# NEXT_PUBLIC_SENTRY_DSN=
# NEXT_PUBLIC_GA_TRACKING_ID=
